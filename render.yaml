services:
  - type: web
    name: ipk-messenger
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server/server-sqlite.js
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
      - key: HOST
        value: "0.0.0.0"
